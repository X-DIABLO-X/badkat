{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the overlay and the settings window. Without the event permissions the frontend cannot listen('bust') / emit(), so the cat never runs its countdown and never closes anything.",
  "windows": ["pet", "settings"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "core:app:default"
  ]
}
