{"default":{"identifier":"default","description":"Core permissions for the overlay, the settings window, and the small hitbox window. Without the event permissions the frontend cannot listen('bust') / emit(), so the cat never runs its countdown and never closes anything; the hitbox window specifically needs emit() to relay clicks to the overlay.","local":true,"windows":["pet","settings","hitbox"],"permissions":["core:default","core:event:default","core:window:default","core:webview:default","core:app:default","updater:default"]}}